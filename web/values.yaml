replicaCount: 1
image:
  repository: nginx
  tag: latest
service:
  port: 80
ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: test.local
      paths:
        - /
namespace: "web"