apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-data
  labels:
    app: data
spec:
  ports:
    - protocol: TCP 
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: data
    tier: data
  type: ClusterIP